
<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirige tout vers index.php si ce n'est pas un fichier ou dossier existant
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
